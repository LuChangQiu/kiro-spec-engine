# v3.6.59 Release Notes

Release date: 2026-03-19

## Highlights

- Published an alignment-only follow-up patch so the npm package, repository tag, and release branch all reference the same final repository state.
- Kept the MagicBall delivery, engineering, and multi-project contracts the same as `v3.6.58`.
- Stabilized tracked project-shared problem projection aging so release/publish no longer dirties the worktree on minute-scale `age_days` drift.

## Validation

- `npm run audit:release-docs`
- `npx jest tests/unit/problem/project-problem-projection.test.js --runInBand`
- `node scripts/git-managed-gate.js --fail-on-violation --json`
- `npm publish --access public` via `prepublishOnly`

## Release Notes

- This patch is still mainly a release-alignment follow-up, but it also fixes the last governance nuisance in that flow: tracked project-shared problem projection files now round `age_days` to whole days, so publish-time projection refresh no longer dirties the repository just because a few minutes passed. Use `v3.6.59` as the canonical version when strict npm/tag/repository alignment matters.
