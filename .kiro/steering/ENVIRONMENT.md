# 环境配置

**项目**: kiro-spec-engine (kse) - Node.js CLI + npm 包  
**环境**: Windows (cmd) | Python 3.8+ | Kiro IDE  
**仓库**: https://github.com/heguangyong/kiro-spec-engine

**目录**: `.kiro/specs/` (Spec) | `.kiro/steering/` (规则) | `.kiro/tools/` (工具)

**发布**: 更新版本 → CHANGELOG → 提交 → `git tag vX.Y.Z` → `git push origin vX.Y.Z` → CI 自动发布

---

v8.0 | 2026-02-05 | 精简 75%
